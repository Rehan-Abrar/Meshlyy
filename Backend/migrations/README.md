# Migrations

Place SQL migration files here (for example `0001_init.sql`).

Guidelines:
- One migration per logical change.
- Never edit applied migrations.
- Keep rollback notes in the migration file comments.
